"""Unit tests — pure logic, no DB, no HTTP.

Each module here tests one focused concern:
    - settings parsing
    - pagination helpers
    - datetime utils
    - error envelope
    - password hashing
"""
